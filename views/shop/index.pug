extends ../layouts/common

block content
  h1 Book Store

  a(href="/shops/books/create") Thêm sách
  
  if !books
    .alert.alert-primary.mt-2 No book in your shop. Please Add!
  else 
      span= books.books.coverUrl
      img(src=books.books.coverUrl)
        
                